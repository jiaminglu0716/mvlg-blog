```ts
/**
 * Q: We need to change this module with dfs search sys.
 * > else, we only define our blogs in the blog directory.
 *
 * ------------------------------------------------
 *  > Slugs (Current) -> test(Error)
 * [
 *   'long-expected-party.md',
 *   'passing-of-grey-company.md',
 *   'prancing-pony.md',
 *   'riders-of-rohan.md',
 *   'test'
 * ]
 * ------------------------------------------------
 *
 * Q: href error
 *
 * ------------------------------------------------
 *  > Error: The provided `as` value (/posts/test/test1) is
 * incompatible with the `href` value (/posts/[slug]). Read more:
 * https://nextjs.org/docs/messages/incompatible-href-as
 * ------------------------------------------------
 */
```
